export enum STATUS {
    TODO = 'todo',
    DONE = 'done',
}

export enum PRIORITY {
    ALL = 'all',
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high',
}

export enum SORT {
    NEWEST_FIRST = 'newest_first',
    DUE_DATE = 'due_date',
    A_Z = 'a_z',
    PRIORITY = 'priority',
}